const chatbotConfig = {
    currentLang: 'ar',
    responses: {
        ar: {
            'ما هي خدماتكم؟': 'نحن متخصصون في تقديم حلول ويب متكاملة تتضمن:\n\n💎 تصميم المواقع التعريفية (Business sites)\n🌟 بورتفوليو احترافية للخبراء والمستقلين\n📈 صفحات الهبوط (Conversion-focused Landing Pages)\n⚡ تحسين الأداء وتجربة المستخدم (UI/UX)\n🔧 صيانة وتأمين المواقع بشكل دوري\n\nنركز على الجودة، السرعة، واللمسة الجمالية العصرية.',
            'كم تكلفة الموقع؟': 'التكلفة تخضع لتقييم دقيق بناءً على متطلبات مشروعك الفريدة:\n\n📊 حجم ونوع المشروع\n💬 الوظائف البرمجية المطلوبة\n🌐 خدمات الدومين والاستضافة\n🎨 التعقيد البصري والتفاعلي\n\nنؤمن بالشفافية، لذا نقدم عرض سعر مخصص (Detailed Quote) بعد جلسة نقاش قصيرة لفهم أهدافك.',
            'كم مدة التنفيذ؟': 'نلتزم بجدول زمني احترافي يضمن الجودة:\n\n⚡ المشاريع البسيطة: 4-6 أيام عمل\n🏢 المواقع المتوسطة: 8-12 يوم عمل\n💼 الأنظمة المتكاملة: 15-25 يوم عمل\n\nنعتمد منهجية "Update-Driven" حيث تطلع على التقدم خطوة بخطوة.',
            'كيف نبدأ العمل؟': 'عملية البدء منظمة وسلسة:\n\n1️⃣ مناقشة أهداف المشروع\n2️⃣ تقديم التصور المبدئي (Briefing)\n3️⃣ التنفيذ التطويري\n4️⃣ المراجعة والتعديلات النهائية\n5️⃣ الإطلاق والدعم\n\nهل تود عقد جلسة نقاش قصيرة لمشروعك؟',
            'ما هي التقنيات المستخدمة؟': 'نستخدم أحدث التقنيات لضمان أداء عالمي:\n\n🚀 HTML5 / Modern CSS / JavaScript\n✨ GSAP للتحريكات المتقدمة\n📱 Responsive Design (لجميع الشاشات)\n🔍 SEO Optimization للظهور في جوجل\n\nكل الكود يتم كتابته بمعايير احترافية وسهولة في الإدارة.',
            'هل يوجد دعم بعد التسليم؟': 'بالتأكيد، الجودة لا تنتهي بالتسليم:\n\n🛡️ فترة دعم فني مجانية للمراجعة\n♻️ تحديثات دورية واستشارات تقنية\n📦 صيانة دورية لضمان استقرار الأداء\n\nنحن شريك تقني مستمر لضمان نجاح موقعك على المدى البعيد.',
            'أريد التواصل معكم': 'يسعدنا جداً البدء في مشروعك! يمكنك التواصل بنا مباشرة عبر:\n\n📱 واتساب: 01115582202\n📧 البريد: web.flow1488@gmail.com\n🌐 أو اترك بياناتك في نموذج التواصل أسفل الموقع!\n\nسنرد عليك في أقرب وقت لبدء العمل الجاد!',
            'default': 'شكراً لتواصلك! 😊\n\nللحصول على المساعدة المثالية، يمكنك اختيار أحد المواضيع السريعة أدناه، أو التواصل معنا مباشرة عبر البريد.\n\nكيف يمكنني دعم مشروعك اليوم؟'
        },
        en: {
            'What are your services?': 'We specialize in professional web solutions including:\n\n💎 Corporate & Business Websites\n🌟 Expert & Freelancer Portfolios\n📈 Conversion-focused Landing Pages\n⚡ UI/UX Performance Optimization\n🔧 Regular Maintenance & Security\n\nWe focus on quality, speed, and modern aesthetics.',
            'How much does a website cost?': 'Pricing is carefully evaluated based on your unique project requirements:\n\n📊 Project scale and type\n💬 Required functionalities\n🌐 Domain and Hosting needs\n🎨 Visual & interactive complexity\n\nWe provide a detailed custom quote after a brief discussion to fully understand your goals.',
            'How long does it take?': 'We maintain a professional timeline that guarantees quality:\n\n⚡ Simple Projects: 4-6 business days\n🏢 Medium Sites: 8-12 business days\n💼 Integrated Systems: 15-25 business days\n\nWe follow an "Update-Driven" methodology so you can track progress every step of the way.',
            'How to start?': 'Our onboarding process is organized and seamless:\n\n1️⃣ Objective Discussion\n2️⃣ Initial Briefing & Moodboard\n3️⃣ Development & Implementation\n4️⃣ Review & Final Adjustments\n5️⃣ Launch & Success Support\n\nWould you like to schedule a brief consultation?',
            'What technologies are used?': 'We use cutting-edge technologies to ensure top performance:\n\n🚀 HTML5 / Modern CSS / JavaScript\n✨ GSAP for advanced animations\n📱 Responsive Design for all devices\n🔍 SEO-ready code structure\n\nAll code is written with professional standards for speed and scalability.',
            'Is there support after delivery?': 'Our commitment extends beyond launch:\n\n🛡️ Free technical support period\n♻️ Regular updates & consultations\n📦 Performance maintenance checks\n\nWe act as your long-term technical partner to ensure your site continues to succeed.',
            'I want to contact you': 'We are excited to start your project! Contact us via:\n\n📱 WhatsApp: 01115582202\n📧 Email: web.flow1488@gmail.com\n🌐 Or use the contact form at the bottom of the page!\n\nWe will get back to you immediately to begin the serious work.',
            'default': 'Thanks for reaching out! 😊\n\nFor best assistance, please select a topic below or contact us directly via email.\n\nHow can I help your project today?'
        }
    }
};

// DOM Elements
const chatbotToggle = document.getElementById('chatbotToggle');
const chatbotWindow = document.getElementById('chatbotWindow');
const closeBtn = document.getElementById('closeBtn');
const messageInput = document.getElementById('messageInput');
const sendBtn = document.getElementById('sendBtn');
const chatMessages = document.getElementById('chatMessages');
const quickReplies = document.querySelectorAll('.quick-reply');
const notificationBadge = document.querySelector('.notification-badge');

// Toggle Chatbot with GSAP
let isChatOpen = false;

chatbotToggle.addEventListener('click', () => {
    if (!isChatOpen) {
        openChat();
    } else {
        closeChat();
    }
});

function openChat() {
    isChatOpen = true;
    chatbotWindow.classList.add('active');

    gsap.fromTo(chatbotWindow,
        { opacity: 0, y: 50, scale: 0.9 },
        { opacity: 1, y: 0, scale: 1, duration: 0.5, ease: "back.out(1.7)" }
    );

    messageInput.focus();
    if (notificationBadge) {
        gsap.to(notificationBadge, {
            scale: 0, opacity: 0, duration: 0.3, onComplete: () => {
                notificationBadge.style.display = 'none';
            }
        });
    }
}

function closeChat() {
    isChatOpen = false;
    gsap.to(chatbotWindow, {
        opacity: 0, y: 20, scale: 0.95, duration: 0.4, ease: "power2.in",
        onComplete: () => chatbotWindow.classList.remove('active')
    });
}

// Close
closeBtn.addEventListener('click', closeChat);

// Send Message
sendBtn.addEventListener('click', () => sendMessage());
messageInput.addEventListener('keypress', (e) => {
    if (e.key === 'Enter') {
        sendMessage();
    }
});

// Quick Replies
quickReplies.forEach(btn => {
    btn.addEventListener('click', () => {
        const message = btn.dataset.message;
        sendMessage(message);
    });
});

// Send Message Function
function sendMessage(customMessage = null) {
    const message = customMessage || messageInput.value.trim();

    if (!message) return;

    // Add user message
    addMessage(message, 'user');

    // Clear input
    messageInput.value = '';

    // Show typing indicator
    showTypingIndicator();

    // Simulate bot response delay
    setTimeout(() => {
        hideTypingIndicator();
        const response = getBotResponse(message);
        addMessage(response, 'bot');
    }, 1000 + Math.random() * 800);
}

// Add Message to Chat
function addMessage(text, sender) {
    const messageDiv = document.createElement('div');
    messageDiv.className = `message ${sender}-message`;

    const time = new Date().toLocaleTimeString('ar-EG', {
        hour: '2-digit',
        minute: '2-digit'
    });

    if (sender === 'bot') {
        messageDiv.innerHTML = `
            <div class="message-avatar">
                <i class="fas fa-robot"></i>
            </div>
            <div class="message-content">
                <p>${text.replace(/\n/g, '<br>')}</p>
                <span class="message-time">${time}</span>
            </div>
        `;
    } else {
        messageDiv.innerHTML = `
            <div class="message-avatar">
                <i class="fas fa-user"></i>
            </div>
            <div class="message-content">
                <p>${text}</p>
                <span class="message-time">${time}</span>
            </div>
        `;
    }

    chatMessages.appendChild(messageDiv);

    // Animate new message
    gsap.from(messageDiv, {
        opacity: 0,
        y: 20,
        x: sender === 'bot' ? -20 : 20,
        duration: 0.4,
        ease: "power2.out"
    });

    chatMessages.scrollTop = chatMessages.scrollHeight;
}

// Show Typing Indicator
function showTypingIndicator() {
    const typingDiv = document.createElement('div');
    typingDiv.className = 'message bot-message typing-message';
    typingDiv.innerHTML = `
        <div class="message-avatar">
            <i class="fas fa-robot"></i>
        </div>
        <div class="message-content">
            <div class="typing-indicator">
                <span class="dot"></span>
                <span class="dot"></span>
                <span class="dot"></span>
            </div>
        </div>
    `;
    chatMessages.appendChild(typingDiv);
    chatMessages.scrollTop = chatMessages.scrollHeight;
}

// Hide Typing Indicator
function hideTypingIndicator() {
    const typingMessage = document.querySelector('.typing-message');
    if (typingMessage) {
        typingMessage.remove();
    }
}

// Get Bot Response (Enhanced logic)
function getBotResponse(message) {
    const lang = chatbotConfig.currentLang;
    const responses = chatbotConfig.responses[lang];

    if (responses[message]) {
        return responses[message];
    }

    const lowerMessage = message.toLowerCase();

    if (lang === 'ar') {
        if (lowerMessage.includes('خدم') || lowerMessage.includes('تقدم')) return responses['ما هي خدماتكم؟'];
        if (lowerMessage.includes('سعر') || lowerMessage.includes('تكلف')) return responses['كم تكلفة الموقع؟'];
        if (lowerMessage.includes('مدة') || lowerMessage.includes('وقت')) return responses['كم مدة التنفيذ؟'];
        if (lowerMessage.includes('بدأ') || lowerMessage.includes('كيف') || lowerMessage.includes('طريقة')) return responses['كيف نبدأ العمل؟'];
        if (lowerMessage.includes('تقني') || lowerMessage.includes('لغة') || lowerMessage.includes('تكنو')) return responses['ما هي التقنيات المستخدمة؟'];
        if (lowerMessage.includes('دعم') || lowerMessage.includes('مشكل') || lowerMessage.includes('صيان')) return responses['هل يوجد دعم بعد التسليم؟'];
        if (lowerMessage.includes('تواصل') || lowerMessage.includes('رقم') || lowerMessage.includes('كلم')) return responses['أريد التواصل معكم'];
        if (lowerMessage.includes('مرحب') || lowerMessage.includes('أهلاً') || lowerMessage.includes('سلام')) {
            return 'مرحباً بك في Web Flow! 👋 يسعدنا جداً مساعدتك في بناء وجودك الرقمي الاحترافي. كيف يمكنني دعمك اليوم؟';
        }
    } else {
        if (lowerMessage.includes('service') || lowerMessage.includes('offer')) return responses['What are your services?'];
        if (lowerMessage.includes('price') || lowerMessage.includes('cost')) return responses['How much does a website cost?'];
        if (lowerMessage.includes('time') || lowerMessage.includes('how long')) return responses['How long does it take?'];
        if (lowerMessage.includes('start') || lowerMessage.includes('how to')) return responses['How to start?'];
        if (lowerMessage.includes('tech') || lowerMessage.includes('language') || lowerMessage.includes('stack')) return responses['What technologies are used?'];
        if (lowerMessage.includes('support') || lowerMessage.includes('help') || lowerMessage.includes('maintain')) return responses['Is there support after delivery?'];
        if (lowerMessage.includes('contact') || lowerMessage.includes('reach') || lowerMessage.includes('call')) return responses['I want to contact you'];
        if (lowerMessage.includes('hi') || lowerMessage.includes('hello')) {
            return 'Welcome to Web Flow! 👋 We are excited to help you build your fast & professional website. How can I assist you today?';
        }
    }

    return responses['default'];
}

// Language Toggle Support
function updateLanguage(lang) {
    chatbotConfig.currentLang = lang;

    document.querySelectorAll('[data-ar]').forEach(element => {
        const text = lang === 'ar' ? element.dataset.ar : element.dataset.en;

        // Add fade effect during change
        gsap.to(element, {
            opacity: 0, duration: 0.1, onComplete: () => {
                if (element.tagName === 'INPUT') {
                    element.placeholder = text;
                } else {
                    element.textContent = text;
                }
                gsap.to(element, { opacity: 1, duration: 0.2 });
            }
        });
    });
}

// Auto-show notification badge after 5 seconds
setTimeout(() => {
    if (!isChatOpen && notificationBadge) {
        notificationBadge.style.display = 'flex';
        gsap.fromTo(notificationBadge, { scale: 0 }, { scale: 1, duration: 0.5, ease: "back.out(2)" });
    }
}, 5000);

console.log('🤖 Professional Chatbot with GSAP initialized!');
